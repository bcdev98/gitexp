def my_square(y):
    """Take a value and returns the squared value

    uses the ** operator
    """
    return (y ** 2)


def my_square2(x):
    """uses the * operator to calculate square
    by x * x
    """
    return (x * x)

print(my_square(42))
print(my_square2(42))
print(my_square(10))
